# * ==============================================================================
# *                              Python
# * ==============================================================================
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
.installed.cfg

# * ==============================================================================
# *                        Virtual Environments
# * ==============================================================================
.venv/
venv/
env/
ENV/

# * ==============================================================================
# *                              Testing
# * ==============================================================================
.pytest_cache/
.coverage
htmlcov/
.tox/

# * ==============================================================================
# *                              Database
# * ==============================================================================
# ? دیتابیس SQLite محلی نباید commit شود - داده واقعی معلم/دانش‌آموز است،
# ? نه بخشی از سورس‌کد. هر توسعه‌دهنده با init_db() خودش می‌سازدش.
*.db
*.sqlite
*.sqlite3

# * ==============================================================================
# *                          Environment / Secrets
# * ==============================================================================
.env
.env.*
!.env.example

# * ==============================================================================
# *                              Editor / OS
# * ==============================================================================
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# * ==============================================================================
# *                          Uploaded Answer Sheet Images
# * ==============================================================================
# ? توضیح: در پیاده‌سازی فعلی (extraction/answer_sheet_extractor.py)، عکس آپلودشده
# ? فقط در حافظه پردازش و بعد از پاسخ دادن به Request دور ریخته می‌شود - هیچ‌جا
# ? روی دیسک ذخیره نمی‌شود، پس فعلاً پوشه‌ای برای ignore کردن وجود ندارد.
# ! اگر در آینده تصمیم به ذخیره تصویر اصلی (برای Audit/بازبینی توسط معلم) گرفته
# ! شد، پوشه آن (مثلاً uploads/) باید همین‌جا اضافه شود.
